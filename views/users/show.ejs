<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= user.username %>'s Recipes</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>

    <h1>Recipes by <%= user.username %></h1>

    <% if (recipes.length > 0) { %>
    <ul>
      <% recipes.forEach((recipe) => { %>
      <li><%= recipe.name %></li>
      <% }) %>
    </ul>
    <% } else { %>
    <p><%= user.username %> has not created any recipes yet.</p>
    <% } %>

    <br />
    <a href="/users">⬅️ Back to Community</a>
  </body>
</html>
